{
  "id": "sys_monitor",
  "name": "系统监控",
  "description": "CPU/GPU/内存/磁盘/网络实时数据监控",
  "level": "simple",
  "triggers": ["系统状态", "CPU", "GPU", "内存", "磁盘", "网络", "温度", "性能"],
  "function_schema": {
    "name": "sys_monitor",
    "description": "获取系统硬件实时状态",
    "parameters": {
      "type": "object",
      "properties": {
        "target": {"type": "string", "enum": ["cpu", "gpu", "memory", "disk", "network", "all"], "description": "监控目标"},
        "format": {"type": "string", "enum": ["summary", "detailed"], "description": "输出格式"}
      },
      "required": ["target"]
    }
  }
}
