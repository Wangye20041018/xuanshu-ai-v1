# 玄枢AI 安装指南

## Windows SmartScreen 拦截说明

玄枢AI 是个人开发的开源应用，使用自签名代码证书进行签名。首次安装或运行时，Windows SmartScreen 可能会弹出"Windows 已保护你的电脑"警告。

**这是正常现象，并非安全威胁。** 请按以下步骤操作：

---

## 一键信任（推荐）

1. 下载并解压玄枢AI 安装包
2. **以管理员身份运行** `install-cert.bat`（位于安装包根目录或 `scripts/` 目录）
3. 在弹出的 UAC 窗口中点击"是"
4. 等待提示"安装完成"
5. 重新运行玄枢AI 安装程序，SmartScreen 将不再拦截

---

## 手动信任（备选方案）

### 方法一：SmartScreen 弹窗手动放行

当看到 "Windows 已保护你的电脑" 提示时：

1. 点击 "更多信息"
2. 点击 "仍要运行"
3. 应用将正常启动

### 方法二：手动导入证书

1. 找到安装包中的 `resources/cert/xuanshu-root.cer` 文件
2. 双击打开证书文件
3. 点击 "安装证书"
4. 选择 "本地计算机" → 下一步
5. 选择 "将所有证书放入下列存储" → 浏览
6. 选择 "受信任的根证书颁发机构" → 确定
7. 完成安装

### 方法三：关闭 SmartScreen（不推荐）

在 Windows 安全中心中暂时关闭 "基于声誉的保护"。

---

## 常见问题

**Q: 这是病毒吗？**
A: 不是。玄枢AI 完全开源，代码托管于 GitHub。SmartScreen 拦截仅因应用未使用付费 EV 代码签名证书，这是所有个人开发者面临的共同问题。

**Q: 自签名证书安全吗？**
A: 安全。自签名证书使用 RSA 4096 位密钥，仅用于验证软件发布者身份（确保安装包未被篡改），不会获取您的任何个人信息。

**Q: 安装证书后可以删除吗？**
A: 可以。在 "管理计算机证书" → "受信任的根证书颁发机构" 中找到 "玄枢AI" 证书，右键删除即可。

---

## 开发者说明

自签名证书由 `scripts/generate-cert.ps1` 生成，`electron-builder.yml` 已配置自动签名流程。
