{
  "id": "quick-launch",
  "name": "快捷启动",
  "description": "启动任意软件/打开文件夹/执行命令",
  "level": "simple",
  "triggers": ["启动", "打开软件", "运行", "执行命令", "打开文件夹", "打开目录"],
  "function_schema": {
    "name": "quick_launch",
    "description": "启动软件或执行命令",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {"type": "string", "enum": ["launch", "open_folder", "run_cmd"]},
        "target": {"type": "string", "description": "软件名/路径/命令"}
      },
      "required": ["action", "target"]
    }
  }
}
